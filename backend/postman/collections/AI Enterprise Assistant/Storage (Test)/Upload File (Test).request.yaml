$kind: http-request
method: POST
url: '{{baseUrl}}/api/v1/test/storage/upload'
order: 1000
description: 'Test endpoint to directly upload a file to MinIO storage. Returns stored file metadata including bucket, objectKey, contentType, and size.'
body:
  type: formdata
  content:
    - key: file
      type: file
      src: ''
      description: The file to upload to MinIO storage
